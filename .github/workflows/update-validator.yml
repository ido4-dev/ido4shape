name: Update spec-format bundle

on:
  repository_dispatch:
    types: [spec-format-published]
  schedule:
    - cron: '0 9 * * 1'  # Weekly Monday 9am UTC (safety net)
  workflow_dispatch:
    inputs:
      version:
        description: 'spec-format version (leave empty for latest)'
        required: false

jobs:
  update-bundle:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Determine target version
        id: version
        run: |
          if [ -n "${{ github.event.client_payload.version }}" ]; then
            echo "target=${{ github.event.client_payload.version }}" >> $GITHUB_OUTPUT
            echo "Source: repository_dispatch"
          elif [ -n "${{ inputs.version }}" ]; then
            echo "target=${{ inputs.version }}" >> $GITHUB_OUTPUT
            echo "Source: workflow_dispatch"
          else
            LATEST=$(npm view @ido4/spec-format version 2>/dev/null || echo "")
            if [ -z "$LATEST" ]; then
              echo "ERROR: Could not fetch latest version from npm"
              exit 1
            fi
            echo "target=$LATEST" >> $GITHUB_OUTPUT
            echo "Source: npm latest ($LATEST)"
          fi

      - name: Check if update needed
        id: check
        run: |
          CURRENT=$(cat dist/.spec-format-version 2>/dev/null || echo "none")
          TARGET="${{ steps.version.outputs.target }}"
          echo "current=$CURRENT" >> $GITHUB_OUTPUT
          echo "target=$TARGET" >> $GITHUB_OUTPUT
          if [ "$CURRENT" = "$TARGET" ]; then
            echo "needed=false" >> $GITHUB_OUTPUT
            echo "Already at v$TARGET — no update needed"
          else
            echo "needed=true" >> $GITHUB_OUTPUT
            echo "Update needed: v$CURRENT → v$TARGET"
          fi

      - name: Update bundle
        if: steps.check.outputs.needed == 'true'
        run: bash scripts/update-validator.sh "${{ steps.version.outputs.target }}"

      - name: Determine merge strategy
        if: steps.check.outputs.needed == 'true'
        id: merge
        run: |
          CURRENT="${{ steps.check.outputs.current }}"
          TARGET="${{ steps.check.outputs.target }}"
          CURRENT_MAJOR=$(echo "$CURRENT" | cut -d. -f1)
          TARGET_MAJOR=$(echo "$TARGET" | cut -d. -f1)
          if [ "$CURRENT_MAJOR" != "$TARGET_MAJOR" ] && [ "$CURRENT" != "none" ]; then
            echo "auto=false" >> $GITHUB_OUTPUT
            echo "label=needs-review" >> $GITHUB_OUTPUT
            echo "Major version change ($CURRENT_MAJOR → $TARGET_MAJOR) — requires review"
          else
            echo "auto=true" >> $GITHUB_OUTPUT
            echo "label=auto-merge" >> $GITHUB_OUTPUT
            echo "Patch/minor update — auto-merge enabled"
          fi

      - name: Create Pull Request
        if: steps.check.outputs.needed == 'true'
        id: pr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.PAT }}
          commit-message: "chore: update spec-validator bundle to v${{ steps.version.outputs.target }}"
          title: "Update spec-validator to v${{ steps.version.outputs.target }}"
          body: |
            Automated bundle update from @ido4/spec-format.

            **Previous version:** ${{ steps.check.outputs.current }}
            **New version:** ${{ steps.version.outputs.target }}

            Bundle has been smoke-tested against the example strategic spec.

            ${{ steps.merge.outputs.auto == 'true' && 'This is a patch/minor update — auto-merge enabled.' || '⚠️ **Major version change** — review validate-spec skill for output format compatibility before merging.' }}
          branch: automated/update-spec-validator
          labels: ${{ steps.merge.outputs.label }}
          delete-branch: true

      - name: Enable auto-merge (fall back to immediate merge if CI completed first)
        if: steps.check.outputs.needed == 'true' && steps.merge.outputs.auto == 'true' && steps.pr.outputs.pull-request-number
        run: |
          # `gh pr merge --auto` requires pending status checks. When CI finishes
          # before this step runs, GitHub returns "Pull request is in clean status"
          # and exits non-zero. That race is not a failure — the PR is already
          # mergeable, so fall back to an immediate squash merge in that case.
          PR=${{ steps.pr.outputs.pull-request-number }}
          set +e
          output=$(gh pr merge "$PR" --auto --squash 2>&1)
          rc=$?
          set -e
          echo "$output"
          if [ "$rc" -ne 0 ]; then
            if echo "$output" | grep -qF 'is in clean status'; then
              echo "PR already mergeable — merging immediately"
              gh pr merge "$PR" --squash --delete-branch
            else
              exit "$rc"
            fi
          fi
        env:
          GH_TOKEN: ${{ secrets.PAT }}
